# Выбор типа БД (mysql, postgresql, sqlite, clickhouse)
DB_TYPE=clickhouse

# Параметры MySQL (оставляем, если будем использовать)
MYSQL_HOST=localhost
MYSQL_PORT=8889
MYSQL_NAME=fpds
MYSQL_USER=root
MYSQL_PASSWORD=root

# Параметры ClickHouse
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123  # HTTP порт по умолчанию
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=692607
CLICKHOUSE_DB=fpds_clickhouse  # Название базы данных
CLICKHOUSE_SECURE=false  # true, если используется HTTPS

# Директория для хранения файлов (паркетов)
DATA_DIR="/Users/iliaoborin/fpds/data/"